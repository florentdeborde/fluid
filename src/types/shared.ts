/**
 * Shared size types for consistency across the Fluid library.
 */
export type ComponentSize = 'sm' | 'md' | 'lg';

/**
 * Shared semantic colors for consistency across the Fluid library.
 */
export type ComponentColor = 'primary' | 'destructive' | 'success' | 'neutral';
